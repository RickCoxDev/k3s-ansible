options: "{{ '--datastore-endpoint \"' + databasetype + '://' + username + ':' + password + '@tcp(' + hostname + ':' + dbport|string + ')/' + dbname + '\" ' if externaldatabase | bool else '' }}{{ '--disable coredns ' if not coredns | bool else '' }}{{ '--disable servicelb ' if not servicelb | bool else '' }}{{ '--disable traefik ' if not traefik | bool else '' }}{{ '--disable local-storage ' if not local_storage | bool else '' }}{{ '--disable metrics-server ' if not metrics_server | bool else '' }}"